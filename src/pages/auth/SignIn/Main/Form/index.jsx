import * as S from "./../../../styles";

import Inputs from "./Inputs";
import SubmitButton from "./SubmitButton";

const Form = () => {
    return (
        <S.Form>
            <Inputs />
            <SubmitButton />
        </S.Form>
    );
};

export default Form;