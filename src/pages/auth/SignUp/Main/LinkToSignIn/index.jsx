import { Link } from "react-router-dom";

const LinkToSignIn = () => {
    return (
        <Link to='/sign-in'>
            Switch back to log in
        </Link>
    );
};

export default LinkToSignIn;